//returns the current URL for our site
export const getPort = () => {
    //if (process.env.NODE_ENV==='development') return 'localhost:3000'
    //return "tdifielddays.com";
    return 'localhost:3000';
};
